import { Scene } from '../Entities/scene';
import { WebGPUDescriptor } from './Utils/utils';

class WebGPURenderer {
    render(scene: Scene) {
        // retrouver chaque bufferGeometry
        // set buffer pour avec tout les attributes
        // draw indexed si présence d'index
    }
}
